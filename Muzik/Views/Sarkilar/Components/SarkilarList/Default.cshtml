@model IEnumerable<Muzik.Models.Sarkilar>


@if (Model.Count() > 0)
{<var class="sarkiListesi">
    @foreach (var item in Model)
    {
     <tr>
         <td>
             <a>  @item.SarkiAd</a> &nbsp;| &nbsp;
                      </td>
         <td>
             <a>     @item.AlbumAd </a>&nbsp;| &nbsp;
         </td>
         <td>
             <a>     @item.Artist</a>&nbsp;| &nbsp;
         </td>
     
         <td>
             @*<a asp-action="Edit" asp-route-id="@item.SarkiId">Edit</a> &nbsp;| &nbsp;*@
             <a asp-action="Details" asp-route-id="@item.SarkiId">Details</a> 
             @*<a asp-action="Delete" asp-route-id="@item.SarkiId">Delete</a>*@
         </td>
     </tr>
        <BR />
    }

</var>
}

else
{
    @:<div> Şarkı bulunamadı.</div>
}
